<p id="notice"><%= notice %></p>

<p>
  <strong>Message:</strong>
  <%= @tweet.message %>
</p>

<!-- changed to username instead of user id that way
it will show the username of the person tweeting -->
<p>
    <strong>User:</strong>
    <%= link_to @tweet.user.username, 
       show_user_path(id: @tweet.user.id) %>
</p>

<%= link_to 'Edit', edit_tweet_path(@tweet) %> |
<%= link_to 'Back', tweets_path %>
